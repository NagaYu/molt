[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    slow: needs real model weights (downloads); excluded by default with -m "not slow"
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
